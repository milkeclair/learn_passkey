<%= form_with scope: :registration, url: registration_path,
  data: { action: "submit->webauthn-register#webauthnStep", webauthn_register_target: "form" } do |f|
%>
  <%= f.label :webauthn %>
  <%= f.submit "submit" %>
<% end %>